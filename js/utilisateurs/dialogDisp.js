// function addClickListener(element, targetDialog) {
//     element.addEventListener('click', event => {
//         setOverlayOnClick(element, overlay);
//         event.preventDefault();
//         dialogSure.style.display = targetDialog === dialogSure ? "block" : "none";
//         dialogAddMod.style.display = targetDialog === dialogAddMod ? "block" : "none";
//     });
// }

// modefier.forEach(mod => addClickListener(mod, dialogAddMod));
// supprimer.forEach(supp => addClickListener(supp, dialogSure));
// addClickListener(addUser,dialogAddMod);